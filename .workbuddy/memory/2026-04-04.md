# 2026-04-04 工作日志

## recognize.py → Tkinter GUI 改造

- 识别出 9 个原始代码问题（全局副作用、中文路径 safe_name 过滤汉字、裸 except、moviepy API 不一致等）
- 将 `KEYWORD_MAPPING` 硬编码迁移至 `./cname/name.json`，格式为 `[{"db_name": "角色名", "aliases": [...]}]`
- 重写 `recognize.py` 为 Tkinter GUI 版本：
  - 窗口默认 1024×768，左侧控制面板 + 右侧预览 + 下方日志
  - 支持拖拽图片/视频（需 tkinterdnd2；无此库时仍可用按钮打开）
  - 模型懒加载（首次用到时才初始化，GUI 不卡死）
  - 视频处理在子线程，支持随时停止
  - 内置别名编辑器（可直接编辑 cname/name.json）
  - 修复 safe_name 保留汉字、路径使用 BASE_DIR 绝对路径
