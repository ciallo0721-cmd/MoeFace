# 2026-04-14 日志

## 审查问题修复
- **recognize.py L162** [P0]：`extract_features_from_image()` 中 cascade 路径由硬编码 `Path(__file__).parent / "lbpcascade_animeface.xml"` 改为 `CASCADE_PATH`，修复 PyInstaller 打包后特征库构建静默失效的 Bug
- **readme.md** [P1-P2] 修复内容：
  - 两处视频链接补 `https://` 前缀（示例章节 + 注意事项章节）
  - 故障排除编号重复的第 5 项改为第 6 项
  - 目录新增「示例」跳转链接，示例章节补锚点和分隔线
  - 注意事项阈值说明新增推荐范围（0.35~0.40 宽松 / 0.45 默认 / 0.50~0.60 严格）
  - 免责声明合并「许可证」章节（移除重复的独立「许可证」标题），并明确禁止爬取 pixiv 等平台
  - 目录结构 `demo/` 注释补空格

## 多维度审查（四角色）
- 对项目进行了完整的创意/内容/代码/模拟用户四角色审查
- **P0 Bug**：`recognize.py` L162 `extract_features_from_image()` 中 cascade 路径硬编码为 `Path(__file__).parent`，PyInstaller 打包后失效，应改用 `CASCADE_PATH`
- **P1 合规**：爬虫爬取 pixiv 等平台违反 robots.txt，需在免责声明中明确禁止；需核查 EXE 是否捆绑了版权图片衍生的特征库
- **P1 文档**：两处视频链接缺 `https://`（91vip.xn--32v.ink）；故障排除编号 5 重复；目录缺「示例」章节入口
- **P2 UX**：建议在 README 增加阈值推荐范围说明（0.35~0.60 分段说明）
- 审查结论：代码整体良好，文档部分通过，内容需整改

## readme 整合更新
- `data/readme.md`：将「限定网站搜索」章节中的 `site:网址` 改为 `site 网址`（去除冒号），示例同步修改
- 根目录 `readme.md`：
  - 目录中新增「更新时间线」跳转链接
  - 末尾新增「更新时间线」表格区块，整合 `ps.txt` 内容（2026-04-12 v3.0 EXE 版本发布说明及下载链接）
