# 2026-04-14 工作日志

## 修复 recognize.py FileNotFoundError

**问题**：`save_database_to_json` 函数写入 JSON 时未创建子目录，导致 `features/争议别当哥哥了/` 目录不存在时报错。

**修复**：在写入前添加 `json_path.parent.mkdir(parents=True, exist_ok=True)` 创建目录。

**涉及文件**：
- `G:\EmoScan Pro\MoeFace\recognize.py` 第 122-123 行

**features/ 目录现状**（3个JSON）：
- ATRI.json
- warma.json
- 争议别当哥哥了/绪山真寻.json
