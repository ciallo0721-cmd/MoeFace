# 2026-04-13 日志

## 修复 recognize.py 语法错误

- **问题**：第 1339 行引号混用 `self.stats["found_names']`（双引号开头、单引号结尾），导致 `SyntaxError: unterminated string literal`
- **修复**：统一改为双引号 `self.stats["found_names"].values()`

## 修复 cname/name.json 语法错误

- **问题**：第 120 行 `"aliases": ["nailong", "奶龙",]` 数组末尾有尾随逗号（trailing comma），标准 JSON 不允许，导致别名配置加载失败
- **修复**：去掉尾随逗号改为 `["nailong", "奶龙"]`

## 修复 cname/name.json 第二处尾随逗号

- **问题**：第 133 行最后一个对象 `}` 后有 `,`，数组末尾不允许，导致再次加载失败
- **修复**：去掉 `},` 改为 `}`，整个文件 JSON 校验通过（共加载角色配置正常）
