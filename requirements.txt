opencv-python>=4.5.0
torch>=1.9.0
torchvision>=0.10.0
facenet-pytorch>=2.5.0
numpy>=1.21.0
pillow>=8.0.0
requests>=2.25.0
beautifulsoup4>=4.9.0
tkinterdnd2>=0.3.0
moviepy>=1.0.3
mediapipe>=0.10.0

# ── AI 识别增强模块（2026-06-29 新增）─────────────────────────────────
# 表情与情绪识别
imgaug>=0.4.0                    # 图像增强

# 语音转文字 + 说话人分离
faster-whisper>=1.0.0            # 语音转录（基于 Whisper）
librosa>=0.10.0                  # 音频特征提取
scikit-learn>=1.0.0              # 说话人聚类

# NSFW 内容检测
# 可选：tensorflow 或 onnxruntime 用于视觉 NSFW 模型
# pip install tensorflow          # 需要 TF 模型时
# pip install onnxruntime         # 需要 ONNX 模型时

# 开发辅助
scipy>=1.7.0

# ── 16项新功能依赖（2026-07-16 新增）────────────────────────────────
# 功能3：系统托盘 + 全局快捷键
pystray>=0.19.0
keyboard>=0.13.5
# 功能6：REST API 服务
flask>=2.0.0
# 功能9：自动更新检查（requests 已存在）
# 功能10：推理分析报告 (PDF)
fpdf2>=2.5.0
matplotlib>=3.5.0
# 功能12：直播集成
# pyvirtualcam>=0.5.0  # 可选，需要 OBS 驱动
# 功能14：模型可解释性
# captum>=0.5.0  # 可选，用于 Grad-CAM